# count_yolo 阶段总结

搁置后从这里恢复。完整对外说明见 `README.md`，新人接手见 `交接说明.md`，AI 见 `AI交接说明.md`。

## 项目在干什么

固定摄像头视频 → 各进口 × 左转/直行/右转 × 车型。当前阶段：PoC 已跑通 **L1 多断面过线** + **本机 Web 控制台**。L3 全矩阵未在远景素材上成立。

## 示例场景

匝道合流，不是标准十字口。远景、桥墩遮挡。视频约 22 分钟（1346s），1920×1080。人工对照 L1 南直行 1533 辆。对向含潮汐车道，过滤靠运动方向，不靠收窄 x。

横岗收费站等新素材在 `videos/` + `jobs/2026-横岗片段1.yaml`（本机 job，不进 git）。

## 五分钟恢复

```powershell
pip install -e ".[runtime]"
.\run.ps1 serve
# http://127.0.0.1:8765 → 选 Job → 试跑 30s

python -m pytest tests/ -q
.\run.ps1 compare --counts examples\counts_L1_south_through_yolov8m.json --level L1
```

GPU 环境：`.env` 的 `COUNT_YOLO_PYTHON`。电自：`COUNT_YOLO_EBIKE_MODEL`。

## L1 逻辑（两道关）

1. **几何**：中心点穿过 `line_counting` 黄线（`crossing_transition`）
2. **运动**：近几帧 y 位移与 `direction` 一致（挡对向）；阈值可在 Web「运动过滤」调

默认：`motion_min_dy_total=1.5`，`motion_min_dy_med=0.25`（旧版 -6/-1 在拥堵慢车易漏计）。

车型：8m 主四轮；truck 按面积分大小货；ebike 权重补摩托。

## Web 控制台（2026-09）

`.\run.ps1 serve` → Job 管理、标定触发、试跑/全量、停止、本机打开 debug。

| 面板 | 管什么 |
|------|--------|
| 检测与跟踪 | YOLO `conf`、ByteTrack `track_buffer` 等；遮挡丢 ID |
| 运动过滤 | `motion_min_dy_*`；慢车过线不计 |

参数写入 job yaml；每次运行快照在 `output/jobs/<id>/job.snapshot.yaml` 与 `tracker.yaml`。

## 全片结果（合同在 examples/）

| 实验 | 南直行合计 | vs GT(1533) |
|------|-----------|-------------|
| yolov8m | 1304 | -14.9% |
| 8m + ebike 拼接 | 1520 | -0.8% |

降 conf 无收益；fuse 单遍未做。

## 踩过的坑

- `opencv-python` 与 headless 共存 → 标定无窗口
- ByteTrack 缺 `lap` → 装完重跑
- 慢车过线不计 → 运动过滤太严（非检测问题）
- 遮挡丢 ID → 加大 `track_buffer`，`vid_stride` 保持 1
- 重启 serve **不用重画线**
- debug mp4v 浏览器播不了 → 本机打开
- L3 在远景素材为 0 → 换素材再验

## 搁置时未做

- `write_excel.py`
- `--mode fuse` 单遍双模型
- `annotate_zones.py` / L3 八区 GUI
- debug 视频 filtered/ended 分色、h264 浏览器播放
- compare 自动报告 markdown

正式项目：换近机位 → Web 标定试跑 → L1 对齐 GT → 再 L3。
