# count_yolo 阶段总结

搁置后从这里恢复。完整对外说明见 `README.md`，新人接手见 `交接说明.md`。

## 项目在干什么

固定摄像头视频 → 各进口 × 左转/直行/右转 × 车型。当前阶段：PoC 已跑通，示例视频只做了 **L1 南进口直行过线**。L3 全矩阵未在本素材上成立。

## 示例场景

匝道合流，不是标准十字口。远景、桥墩遮挡。视频约 22 分钟（1346s），1920×1080。人工对照只有 6 条进口×转向，L1 主项是南直行 1533 辆（由高峰 pcu/h 外推）。对向含潮汐车道，过滤必须靠运动方向，不能靠收窄 x。

## 五分钟恢复

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -e ".[dev]"
python -m pytest tests/ -q

pip install -e ".[runtime]"
.\run.ps1 8m --mode line --line L1_南直行 --device 0 --max-seconds 120
.\run.ps1 compare --counts examples\counts_L1_south_through_yolov8m.json --level L1
```

已有 CUDA Python 时，把可执行文件写进 `.env` 的 `COUNT_YOLO_PYTHON`。电自权重路径写 `COUNT_YOLO_EBIKE_MODEL`。

## L1 逻辑

计数线：`configs/文锦北路-草埔立交匝道_临时.json` 里 `[[309, 798], [1646, 794]]`，`near_to_far`，`require_motion_direction: true`。过线时近几帧 `dy_total < -6` 且中位步长 `dy_med < -1`。

yolov8m：car / bus / motorcycle 直映；truck 按框面积 >1.2% 画面分为大货否则小货。专用电自权重：Electric-bicycle / bicycle → 摩托，van → 小货，person 不计。

## 全片结果（合同在 examples/）

墙钟约 11–15 分钟 / 次（4080 Super）。

| 实验 | 南直行合计 | vs GT(1533) |
|------|-----------|-------------|
| yolov8m conf=0.25 | 1304 | -14.9% |
| yolov8m conf=0.15 | 1304 | -14.9% |
| ebike 单模型 | 1112 | -27.5% |
| 8m 四轮 + ebike 摩托（事后加总） | 1520 | -0.8% |

拼接口径：car 1132、truck_small 51、bus 119 来自 8m；motorcycle 218 来自 ebike。未做帧级融合。

结论：8m 可作主计数，缺摩托；ebike 不能当主计数；拼接值得在正式项目里做成单遍 fuse；降 conf 没有收益。

## 踩过的坑

- `opencv-python` 与 headless 共存则标定窗口失效
- ByteTrack 缺 `lap` 时要装完重跑
- 对向误计用方向过滤，不要收窄线
- 本视频 L3 为 0 是远景断轨，换素材再验
- GT 断面和近场视野可能不完全重合，-15% 里有口径差

## 搁置时未做

- `write_excel.py`
- `--mode fuse` 单遍双模型
- `annotate_zones.py`
- 优质十字口 + 分车型 GT

正式项目路径：换更近的机位 → 先 L1 对齐 → 再 L3；模型仍是 8m 主计数、专用权重补摩托。
