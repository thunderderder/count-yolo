Place YOLO weights here. They are gitignored.

- `yolov8m.pt` — default COCO detector. Ultralytics can download it on first run if this file is missing.
- `electri_bike_and_vehicle.pt` — optional custom weights for motorcycle / e-bike complement. Set `COUNT_YOLO_EBIKE_MODEL` if the file lives elsewhere.
