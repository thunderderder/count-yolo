# count_yolo 项目交接说明

面向从零接手的人。隔一段时间回来的原作者看 `阶段总结.md` 更快。

## 这是什么

交通调查里对着路口录像数车、分转向、分车型、填 Excel。本仓库用 YOLO + 跟踪 + 几何规则把这件事做成可重复脚本。不是产品。输入是 MP4 和路口 config，输出是 JSON，写 Excel 还没做。

YOLO 只负责认车。转向靠「从哪条线/哪个区来、往哪去」，不靠车头朝向。每辆车只计一次。

## 两种模式

L1 `--mode line`：近场一条线，当前主用，已在示例视频上跑通全片。L3 `--mode od`：八个多边形 + 转向表，代码有骨架，示例视频因远景遮挡不适合验收。

分层：先 L1 证明检测和计数链路，再考虑 L2/L3。L1 通过不能推出 L3。

模型：yolov8m 擅长四轮，摩托几乎看不到；电自专用权重相反。推荐以后做 fuse，现在只能跑两遍事后加总。

## 已有文件

| 路径 | 作用 |
|------|------|
| `src/count_yolo/` | 源码 |
| `run.ps1` / `scripts/run.sh` | 入口：`annotate` / `count` / `8m` / `ebike` / `compare` |
| `configs/` | 示例标定 |
| `ground_truth/` | 人工对照 |
| `examples/` | 已发表全片 JSON |

尚未实现：写 Excel、单遍 fuse、L3 标定 GUI。

示例视频全片南直行：8m = 1304（GT 1533，-14.9%）；拼接摩托后 1520（-0.8%）。细节在 `阶段总结.md`。

## 环境

推荐 NVIDIA GPU，显存 ≥8GB。标定需要本机显示器。Windows 用 `run.ps1`，其它系统用 `scripts/run.sh`。

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -e ".[dev]"
python -m pytest tests/ -q
pip install -e ".[runtime]"
```

不要同时装 `opencv-python` 和 `opencv-python-headless`。不要把权重和视频提交进 git。`yolov8m.pt` 放到 `models/`，或让 ultralytics 首次自动下载。电自权重用环境变量 `COUNT_YOLO_EBIKE_MODEL`。

## 第一次跑（约 30 分钟）

把示例 MP4 放到仓库根目录（文件名保持 `文锦北路-草埔立交匝道临时.MP4`，或显式传 `--video`）。

```powershell
.\run.ps1 8m --mode line --line L1_南直行 --device 0 --max-seconds 120
.\run.ps1 compare --counts examples\counts_L1_south_through_yolov8m.json --level L1
```

全片约 15 分钟。复现合同 1304 用 `examples/counts_L1_south_through_yolov8m.json`。

## 建议工作顺序

1. pytest 全绿，120 秒冒烟
2. 读 `src/count_yolo/geometry.py` 和 `pipeline.py` 的过线逻辑
3. 需要出表再写 Excel 映射
4. 需要摩托补全再做 fuse
5. 换标准十字口视频再做 L3

## 常见问题

| 现象 | 处理 |
|------|------|
| `cuda: False` | 驱动，或 `COUNT_YOLO_PYTHON` 指到 GPU 环境 |
| 标定窗口打不开 | 卸 headless，只留 `opencv-python` |
| 缺 `lap` | 装完重跑同一条命令 |
| 计数偏高 | 查对向车，确认 `require_motion_direction` |
| 计数偏低 | 线是否画到桥墩后；GT 是否含视野外车道 |
| L3 全 0 | 换素材或回到 L1 |
