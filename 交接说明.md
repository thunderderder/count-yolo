# count_yolo 交接说明

给从零接手的人。隔一阵回来的原作者看 [`阶段总结.md`](阶段总结.md) 更快；AI 代跑看 [`AI交接说明.md`](AI交接说明.md)。

## 这是什么

交通调查：对着路口录像数车、分车型、填表。本仓库用 **YOLO + ByteTrack + 计数线几何** 把流程脚本化。输入是 MP4 + 标定 config，输出是 JSON（写 Excel 尚未实现）。

## 推荐工作方式（2026-09 起）

**优先用 Web 控制台**，本机浏览器 + 本机 GPU：

```powershell
.\run.ps1 serve
# http://127.0.0.1:8765
```

| 步骤 | 操作 |
|------|------|
| 1 | 新建 Job（命名如 `2026-路口名_调查项_0901`） |
| 2 | 填 **视频路径**（相对仓库根，如 `videos/xxx.MP4`） |
| 3 | 填 **断面名称**（逗号分隔，如 `L1_主路,L1_匝道`） |
| 4 | 点 **标定断面** → OpenCV 画线 → 自动写入 config |
| 5 | 勾选 **试跑 30 秒** → 跑通后取消勾选 → **全量计数** |
| 6 | 右侧 **本机打开** debug 视频 / 打开输出文件夹 |

标定**每次覆盖** config 里全部 `line_counting`，不保留旧线名。计数**自动统计 config 中全部断面**，无需再勾选。

产物目录：`output/jobs/<job_id>/`

- `counts_<断面>_8m.json`、`counts_all_lines_8m.json`
- `debug_<断面>_8m.mp4`（橙=已过线，绿=跟踪中；左上角 `counted=` 为累计过线数）
- `calibration_preview_10s.mp4`、`run.log`

CLI 仍可独立使用，与 Web 共用同一套 Job yaml。

## 路径规则

- **提交到 git 的配置一律用相对路径**（相对 `count_yolo` 仓库根）。
- 本机绝对路径可用，但不要写进要推送的 Job/config。
- Job 模板：[`jobs/_template.yaml`](jobs/_template.yaml)
- 字段说明：[`docs/input_config.md`](docs/input_config.md)

示例：

```yaml
video: videos/2026-文锦北路.MP4
config: configs/文锦北路.json
lines:
  - L1_主路
  - L1_匝道
preview_seconds: 30   # 试跑；删掉或留空则全片
device: auto          # 无 CUDA 时 Web 会提示并回落 cpu
```

## 命令速查

```powershell
.\run.ps1 serve                              # Web 控制台
.\run.ps1 annotate --config configs\x.json --lines "L1_A,L1_B" --job <job_id>
.\run.ps1 run-job --job <job_id> --device 0  # 按 Job 全量/试跑
.\run.ps1 8m-all --job jobs\xxx.yaml         # 传统 CLI
.\run.ps1 compare --counts output\...\counts_xxx.json --level L1
python -m pytest tests/ -q
```

`run-job` 与 Web「全量计数」走同一逻辑；`preview_seconds: 30` 时只处理前 30 秒。

## 环境与坑

- Python ≥ 3.10；跑视频需 `pip install -e ".[runtime]"`。
- GPU 推荐；`device: auto` 会检测 CUDA。
- 标定需 **本机显示器**；`opencv-python` 与 `headless` **不要同时装**。
- Web 子进程通过 `annotate_line.py` / `count_traffic.py` 启动，依赖仓库根下 `src/`，与 `run.ps1` 一致。
- 缺 `lap`：装上后重跑同一条命令。
- debug 视频为 `mp4v`，浏览器常播不了；用 Web **本机打开** 或资源管理器进 `output/jobs/...`。

`.env`：可复制 `.env.example`；`COUNT_YOLO_PYTHON` 可指向已有 GPU venv。

## 源码地图

| 路径 | 作用 |
|------|------|
| `src/count_yolo/pipeline.py` | YOLO 跟踪 + 过线计数 + debug 渲染 |
| `src/count_yolo/annotate.py` | OpenCV 标定写 config |
| `src/count_yolo/jobs.py` / `run_job.py` | Job yaml 与一次跑数 |
| `src/count_yolo/web/` | FastAPI + 静态页 |
| `src/count_yolo/paths.py` | 相对/绝对路径解析 |

## 分层与精度

- **L1 过线**：当前主用，已在示例匝道视频上验收。
- **L3 OD**：代码骨架在，示例远景素材不适合验收，勿当卖点。
- 摩托漏检：COCO 权重弱；可开 Job 里 `ebike_enabled` 用第二权重再跑一遍（每断面多一个 `debug_*_ebike.mp4`）。单遍 fuse 未做。

## 尚未实现

- 写 Excel 统计表
- 8m + ebike **单遍** fuse
- L3 八区标定 GUI

## 建议接手顺序

1. `pytest` 全绿
2. Web 走通：标定 → 试跑 30s → 看 debug 与 JSON
3. 读 `pipeline.py` 过线与 `geometry.py`
4. 需要出表再做 Excel；需要摩托再规划 fuse
5. 换标准十字口素材再碰 L3
